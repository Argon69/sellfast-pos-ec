<Window x:Class="SellFast.App.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="SellFast POS — Acceso al Sistema"
        Height="620" Width="460"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Icon="pack://application:,,,/Resources/app.ico">

    <Window.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding LoginCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
    </Window.InputBindings>

    <Border CornerRadius="24" Background="{StaticResource BgCanvasBrush}" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="1">
        <Grid>
            <!-- Main Content Container -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="360">

                <!-- Header Logo & Branding -->
                <Border Width="56" Height="56" CornerRadius="16" Background="{StaticResource AccentLimeBrush}" HorizontalAlignment="Center" Margin="0,0,0,16">
                    <TextBlock Text="⚡" FontSize="28" Foreground="{StaticResource SidebarBgBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <TextBlock Text="SellFast POS" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="Sistema de Punto de Venta Inteligente" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,28"/>

                <!-- Card Container (Clean White Card) -->
                <Border Background="White" CornerRadius="20" Padding="28" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Iniciar Sesión" FontSize="17" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>

                        <!-- Username Input -->
                        <TextBlock Text="USUARIO" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource ModernTextBox}" Margin="0,0,0,16"/>

                        <!-- Password Input -->
                        <TextBlock Text="CONTRASEÑA" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <PasswordBox x:Name="TxtPassword" Style="{StaticResource ModernPasswordBox}" PasswordChanged="PasswordBox_PasswordChanged" Margin="0,0,0,16"/>

                        <!-- Error Message -->
                        <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorRedBrush}" FontSize="12" 
                                   HorizontalAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,12"
                                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}, FallbackValue=Collapsed}"/>

                        <!-- Login Button -->
                        <Button Content="Ingresar al Sistema" 
                                Style="{StaticResource PrimaryButton}" 
                                Command="{Binding LoginCommand}" 
                                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                                Margin="0,8,0,0" Height="44"/>
                    </StackPanel>
                </Border>

                <!-- Footer & Close Button -->
                <Button Content="Cerrar Aplicación" Style="{StaticResource GhostButton}" Click="CloseButton_Click" HorizontalAlignment="Center" Margin="0,20,0,0"/>

            </StackPanel>

            <!-- Window Drag Area Top -->
            <Grid VerticalAlignment="Top" Height="40" Background="Transparent" MouseDown="Window_MouseDown"/>
        </Grid>
    </Border>
</Window>
