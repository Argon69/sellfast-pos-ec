<UserControl x:Class="SellFast.App.Views.Pages.ReportesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1100">

    <Border Padding="24">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Filters Bar -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,0">
                        <TextBlock Text="Desde: " Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <DatePicker SelectedDate="{Binding FechaInicio}" Height="36" Width="140"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Hasta: " Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <DatePicker SelectedDate="{Binding FechaFin}" Height="36" Width="140"/>
                    </StackPanel>

                    <StackPanel Grid.Column="3" Orientation="Horizontal">
                        <Button Content="📊 Exportar Excel (.xlsx)" Style="{StaticResource PrimaryButton}" Command="{Binding ExportarExcelCommand}" Padding="16,8" Margin="0,0,8,0"/>
                        <Button Content="📥 Exportar CSV" Style="{StaticResource GhostButton}" Command="{Binding ExportarCsvCommand}" Padding="16,8"/>
                    </StackPanel>
                </Grid>

                <!-- Period KPI Summary -->
                <UniformGrid Columns="2" Margin="-6,0,-6,20">
                    <Border Style="{StaticResource KpiCard}">
                        <StackPanel>
                            <TextBlock Text="TOTAL VENTAS PERÍODO" Style="{StaticResource SectionTitle}" Margin="0,0,0,8"/>
                            <TextBlock Text="{Binding TotalPeriodo, Converter={StaticResource CurrencyFormatterConverter}}" FontSize="26" FontWeight="Bold" Foreground="{StaticResource SuccessGreenBrush}"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource KpiCard}">
                        <StackPanel>
                            <TextBlock Text="TRANSACCIONES COMPLETADAS" Style="{StaticResource SectionTitle}" Margin="0,0,0,8"/>
                            <TextBlock Text="{Binding TransaccionesPeriodo}" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- LiveCharts2 Sales Trend Chart -->
                <Border Background="White" CornerRadius="20" Padding="20" Margin="0,0,0,20" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Evolución de Ventas ($)" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,16"/>
                        <Border Height="260">
                            <lvc:CartesianChart Series="{Binding SalesSeries}" XAxes="{Binding XAxes}"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Detailed Transactions Table with PDF Reprint Button -->
                <Border Background="White" CornerRadius="20" Padding="16" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Historial de Ventas &amp; Comprobantes PDF" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,16"/>
                        
                        <ItemsControl ItemsSource="{Binding TransaccionesLista}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="0,0,0,1" Padding="8,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="150"/>
                                                <ColumnDefinition Width="140"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="130"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Order Number -->
                                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding NumeroTransaccion, StringFormat='{}#{0}'}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                <TextBlock Text="{Binding FechaHora, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                            </StackPanel>

                                            <!-- Payment Method -->
                                            <Border Grid.Column="1" CornerRadius="8" Background="{StaticResource BgPillBrush}" Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding TipoPago}" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                            </Border>

                                            <!-- Client -->
                                            <TextBlock Grid.Column="2" Text="{Binding Cliente.NombreCompleto, FallbackValue='Cliente General'}" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>

                                            <!-- Total -->
                                            <TextBlock Grid.Column="3" Text="{Binding Total, Converter={StaticResource CurrencyFormatterConverter}}" FontSize="14" FontWeight="Bold" Foreground="{StaticResource SuccessGreenBrush}" VerticalAlignment="Center"/>

                                            <!-- PDF Action Button -->
                                            <Button Grid.Column="4" Content="📄 Ver PDF" Style="{StaticResource GhostButton}"
                                                    Command="{Binding DataContext.ReimprimirPdfCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}" Padding="12,6"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>
