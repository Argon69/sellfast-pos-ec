<UserControl x:Class="SellFast.App.Views.Pages.CajaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1100">

    <Border Padding="24">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>

            <!-- Sales Breakdowns & Status -->
            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                <!-- Status Banner -->
                <Border CornerRadius="20" Background="White" Padding="20" Margin="0,0,0,20" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="1">
                    <Grid>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="ESTADO DE CAJA" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock Text="{Binding TextoEstadoCaja}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4,0,0"/>
                        </StackPanel>

                        <Border CornerRadius="10" Background="{StaticResource SuccessGreenBgBrush}" Padding="12,6" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <TextBlock Text="{Binding IsCajaAbierta, Converter={StaticResource StringToVisibilityConverter}, FallbackValue='🟢 Abierta'}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource SuccessGreenBrush}"/>
                        </Border>
                    </Grid>
                </Border>

                <!-- Sales Cards Grid -->
                <UniformGrid Columns="3" Margin="-6,0,-6,20">
                    <Border Style="{StaticResource KpiCard}">
                        <StackPanel>
                            <TextBlock Text="VENTAS EFECTIVO" Style="{StaticResource SectionTitle}" Margin="0,0,0,8"/>
                            <TextBlock Text="{Binding VentasEfectivoHoy, Converter={StaticResource CurrencyFormatterConverter}}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource SuccessGreenBrush}"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource KpiCard}">
                        <StackPanel>
                            <TextBlock Text="VENTAS TARJETA" Style="{StaticResource SectionTitle}" Margin="0,0,0,8"/>
                            <TextBlock Text="{Binding VentasTarjetaHoy, Converter={StaticResource CurrencyFormatterConverter}}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource KpiCard}">
                        <StackPanel>
                            <TextBlock Text="TOTAL VENTAS" Style="{StaticResource SectionTitle}" Margin="0,0,0,8"/>
                            <TextBlock Text="{Binding TotalVentasHoy, Converter={StaticResource CurrencyFormatterConverter}}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>
            </StackPanel>

            <!-- Cash Audit & Actions Panel -->
            <Border Grid.Column="1" Background="White" CornerRadius="20" Padding="20" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Apertura, Cierre &amp; Arqueo" FontSize="17" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>

                        <!-- Opening Box Section -->
                        <Border Margin="0,0,0,20" Padding="0,0,0,16" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="0,0,0,1">
                            <StackPanel>
                                <TextBlock Text="APERTURA DE CAJA" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8"/>
                                <TextBlock Text="BASE INICIAL APERTURA ($)" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                                <TextBox Text="{Binding MontoInicialApertura, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,12"/>
                                <Button Content="🟢 Abrir Nueva Caja" Style="{StaticResource PrimaryButton}" Command="{Binding AbrirCajaCommand}"/>
                            </StackPanel>
                        </Border>

                        <!-- Closing Box Section -->
                        <StackPanel>
                            <TextBlock Text="ARQUEO Y CIERRE DE CAJA" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8"/>
                            <TextBlock Text="MONTO RECONTADO EN CAJA ($)" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                            <TextBox Text="{Binding MontoContadoCierre, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,12"/>

                            <TextBlock Text="OBSERVACIONES" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                            <TextBox Text="{Binding ObservacionesCierre, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Height="70" TextWrapping="Wrap" Margin="0,0,0,16"/>

                            <Button Content="🔴 Cerrar &amp; Realizar Arqueo" Style="{StaticResource DarkButton}" Command="{Binding CerrarCajaCommand}"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</UserControl>
