<UserControl x:Class="SellFast.App.Views.Pages.FichosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1100">

    <Border Padding="24">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>

            <!-- Table & KPIs -->
            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                <!-- Date Filter -->
                <Grid Margin="0,0,0,16">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Fecha de Servicio: " Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <DatePicker SelectedDate="{Binding FechaFiltro}" Height="36" Width="140"/>
                    </StackPanel>
                </Grid>

                <!-- Stats Cards Grid -->
                <UniformGrid Columns="3" Margin="-6,0,-6,16">
                    <Border Style="{StaticResource KpiCard}">
                        <StackPanel>
                            <TextBlock Text="EMITIDOS" Style="{StaticResource SectionTitle}" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding TotalEmitidos}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource KpiCard}">
                        <StackPanel>
                            <TextBlock Text="PENDIENTES" Style="{StaticResource SectionTitle}" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding Pendientes}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource WarningOrangeBrush}"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource KpiCard}">
                        <StackPanel>
                            <TextBlock Text="USADOS" Style="{StaticResource SectionTitle}" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding Usados}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource SuccessGreenBrush}"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- Fichos Table -->
                <Border Background="White" CornerRadius="20" Padding="16" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="1">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Fichos}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="0,0,0,1" Padding="8,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="140"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="110"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="{Binding Numero}" FontSize="13" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>

                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Cliente.NombreCompleto}" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                <TextBlock Text="{Binding FechaSolicitud, StringFormat='{}Emitido: {0:HH:mm}'}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                            </StackPanel>

                                            <Border Grid.Column="2" CornerRadius="8" Background="{StaticResource BgPillBrush}" Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Estado}" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                            </Border>

                                            <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                                                <Button Content="Marcar Usado" Style="{StaticResource GhostButton}" Command="{Binding DataContext.MarcarComoUsadoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="0,0,4,0"/>
                                                <Button Content="Cancelar" Style="{StaticResource GhostButton}" Foreground="{StaticResource ErrorRedBrush}" Command="{Binding DataContext.CancelarFichoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </StackPanel>

            <!-- Issue Ticket Panel -->
            <Border Grid.Column="1" Background="White" CornerRadius="20" Padding="20" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Emitir Nuevo Ficho" FontSize="17" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>

                        <TextBlock Text="SELECCIONAR CLIENTE" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <ComboBox ItemsSource="{Binding ClientesDisponibles}" SelectedItem="{Binding SelectedClienteParaFicho}" DisplayMemberPath="NombreCompleto" Height="38" Margin="0,0,0,20"/>

                        <Button Content="🎫 Generar &amp; Emitir Ficho" Style="{StaticResource PrimaryButton}" Command="{Binding EmitirFichoCommand}" Padding="20,12"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</UserControl>
